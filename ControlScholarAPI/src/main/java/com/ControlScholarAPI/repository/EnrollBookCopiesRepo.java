package com.ControlScholarAPI.repository;

import com.ControlScholarAPI.model.EnrollBookCopies;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EnrollBookCopiesRepo extends JpaRepository<EnrollBookCopies, Integer> {
}
